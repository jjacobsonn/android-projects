<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jjinvestmenttracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JJInvestmentTracker">

        <!-- Displays Main Activity (The Launcher Activity) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Detail Activity is used for displaying investment details -->
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false" />

        <!-- Adds the Investment Activity which is used for adding new investments -->
        <activity
            android:name=".ui.AddInvestmentActivity"
            android:exported="false" />
    </application>
</manifest>